export const metadata = {
  title: "Setup Guide — BQ Duplicate Checker",
  description:
    "Step-by-step guide to run the BQ Duplicate Checker locally with your own BigQuery data.",
};

export default function GuideLayout({ children }) {
  return children;
}
