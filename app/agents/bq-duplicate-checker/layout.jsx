export const metadata = {
  title: "BQ Duplicate Checker — Abhimanyu Sheoran",
  description:
    "Check any BigQuery table for duplicate rows at a user-defined grain. Includes a CTE-by-CTE SQL debugger.",
};

export default function DuplicateCheckerLayout({ children }) {
  return children;
}
