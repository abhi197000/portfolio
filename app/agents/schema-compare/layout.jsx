export const metadata = {
  title: "Schema Comparison Agent — Abhimanyu Sheoran",
  description:
    "Compare BigQuery table schemas across multiple GCP projects and datasets. Identify column mismatches, type differences, and missing fields.",
};

export default function SchemaCompareLayout({ children }) {
  return children;
}
