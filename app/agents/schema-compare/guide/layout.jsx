export const metadata = {
  title: "Setup Guide — Schema Comparison Agent",
  description:
    "Step-by-step guide to run the Schema Comparison Agent locally with your own BigQuery data.",
};

export default function GuideLayout({ children }) {
  return children;
}
